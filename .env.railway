# Copy to .env for local development

# Postgres connection string (Railway will inject DATABASE_URL automatically)
DATABASE_URL=postgres://user:password@host:port/dbname

# Port for local dev (Railway will inject PORT)
PORT=8080

# Optional: base URL for generating full short links (for local testing)
BASE_URL=http://localhost:8080
